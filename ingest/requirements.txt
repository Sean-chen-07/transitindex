# OPTIONAL real-I/O dependencies. None of these are needed to run the test
# suite (cd ingest && python -m pytest) -- core logic is pure stdlib and the
# real-I/O modules import their deps lazily. Install when wiring a real path.
psycopg[binary]>=3.2     # PostgresRepository -> writes core.* in Postgres
anthropic>=0.40          # LLM-assisted extraction
pdfplumber>=0.11         # PDF table extraction
fastapi>=0.115           # review API
httpx>=0.27              # source fetching
python-dotenv>=1.0       # config.py .env loading (stdlib fallback exists)
