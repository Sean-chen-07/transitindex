"""Recompute / maintenance jobs that run over already-promoted metric values.

Currently: `derived_recompute` -- recomputes the 6 derived ratio metrics from
their same-period source inputs and writes them as first-class metric values.
"""
